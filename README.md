A demonstration of utilizing a simple neural network to play the game Flappy Bird using pygame and NEAT-python!

## How to build the project:
In order to build the project, make sure `pygame` and `neat-python` is installed. Then, type `python main.py` in the terminal.
